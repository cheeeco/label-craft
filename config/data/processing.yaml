# Dataset configuration
data:
  batch_size: 16
  max_length: 256
  test_size: 0.2
  val_size: 0.1
  training_size: 0.1  # Fraction of data to use for training (0.1 = 10%)
  max_samples_per_category: 1000  # Maximum samples per category to prevent imbalance
  num_workers: 4  # Number of worker processes for data loading
